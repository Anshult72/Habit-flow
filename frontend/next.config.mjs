/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel deploys Next.js natively — no static export needed.
  // Server-side features (API routes, SSR) are now available.
  images: { unoptimized: true },
};

export default nextConfig;
